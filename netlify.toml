# Netlify build config. Netlify builds the app in the cloud on every push to
# GitHub — nothing needs installing on your PC.
[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: send all routes back to index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
