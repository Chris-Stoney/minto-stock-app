# Copy to .env.local and fill in once we set up Supabase (Track 2, milestone 3).
# These are project keys, safe for the browser (the anon key is meant to be public;
# real protection comes from Supabase Row Level Security, which we'll configure).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
